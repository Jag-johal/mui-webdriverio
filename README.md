## Local App Setup:
- Install dependencies: `npm install`
- Start app: `npm start`

## How to run tests:-
- Access test folder: `cd e2eTests`
- Install dependencies: `npm install`
- Run tests: `npm test`